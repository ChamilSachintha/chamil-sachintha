# Project Rules

- After completing any code modification or feature implementation, always commit the changes to Git with a relevant, descriptive commit message (e.g. `feat: ...`, `fix: ...`, `refactor: ...`).
